# Mystral — прогресс разработки

## 2026-07-06 — ТЗ-060 доп. фикс #2: тот же обрыв, но уже без «раздумий вслух»

### Completed
- Прод после фикса #1: снова `Expecting ',' delimiter`, но контент чистый (intro+10 секций+5 FAQ, без саморассуждений — анти-rambling сработала). Обрыв на последнем поле `cta_text`.
- Причина неизвестности: `finish_reason` от API не логировался — не было прямого сигнала "обрыв по лимиту токенов vs другая ошибка".
- `GENERATION_MAX_TOKENS`: 4096 → **6144**.
- Логирование `finish_reason`: если `"length"` — WARNING до попытки парсинга.

### Problems found
- tiktoken-оценка (cl100k_base, прокси для токенизатора DeepSeek) легла в диапазон, где 4096 могло не хватить впритык — цифра ненадёжна, см. фикс #3.

### Verified
- Новый тест в `test_seo_generator.py` (13→14): мок `finish_reason="length"` + обрезанный JSON — проверяет лог и контролируемый fallback. Полный `pytest` — 139 passed, 2 skipped.
- Прод-прогон `--langs en,tr --types zodiac --slugs aries,taurus,gemini`: **3 generated, 3 skipped, 0 failed**. ТЗ-060 закрыто (на тот момент) — можно запускать оставшиеся ~600 страниц.

### Next step
- `git pull && ./deploy.sh`. Если `Expecting ',' delimiter` вернётся: `finish_reason=length` в логе → токен-бюджет (поднять лимит ещё); без него → другой класс ошибки.

---

## 2026-07-06 — ТЗ-060 доп. фикс #3: 6144 тоже не хватило

### Completed
- Полный прогон 615 страниц: после 6 успешных en-страниц — `zodiac/capricorn/en` обрезан, `finish_reason=length` в логе подтвердил причину (сигнал из фикса #2 сработал).
- `GENERATION_MAX_TOKENS`: 6144 → **12000** (сразу щедрый запас, не по чуть-чуть). `max_tokens` — потолок, не стоимость: платится по факту сгенерированного.

### Problems found
- tiktoken-оценка по смещению обрыва дала ~3–4.5k токенов — противоречит факту обрыва при лимите 6144. Вывод: cl100k_base недостаточно точно аппроксимирует токенизатор DeepSeek для таких расчётов — дальше полагаться на эмпирику (реальные обрывы), не на вычисления.

### Verified
- Регрессионный тест на порог `GENERATION_MAX_TOKENS >= 12000`. Полный `pytest` — 139 passed, 2 skipped.

### Next step
- `git pull && ./deploy.sh`, продолжить полный прогон (resume пропустит уже успешные страницы): `docker compose exec backend python scripts/generate_seo_translations.py`. Обрыв с `finish_reason=length` → лимита всё ещё не хватает, прислать лог; без него → другой класс проблемы.

---

## 2026-07-07 — ТЗ-037c/ТЗ-060: полная батч-генерация завершена

### Completed
- Прод: **577 generated, 38 skipped, 0 failed** — все 615 страниц (123 × 5 языков), ноль сбоев парсинга JSON. 38 skipped — уже сгенерированные в пробных прогонах, resume отработал корректно.
- Все три уровня фиксов ТЗ-060 (`response_format=json_object`, прогрессия `GENERATION_MAX_TOKENS` 3000→4096→6144→12000, анти-rambling промпт, логирование `finish_reason`) в сумме дали 0% отказов на полном объёме.
- ТЗ-037c закрыто по контенту.

### Next step
- Ручное (вне доступа Claude Code): проверить несколько страниц curl'ом (`/es/zodiac/aries`, `/tr/tarot/the-fool`, `/uk/runes/fehu`) на язык контента; отправить обновлённый `sitemap.xml` в Яндекс.Вебмастер и Google Search Console.
- Alembic миграции (вместо create_all); тестирование уведомлений на VPS; перепроверить `shareToTelegram` на телефоне; webhook URL ЮKassa в личном кабинете; честный per-page lastmod в sitemap — бэклог; уникальный индекс `(page_type, slug, lang)` от гоночных дублей — бэклог.

---

## 2026-07-07 — ТЗ-061: возврат средств не отзывал Pro-статус (критический баг)

### Completed
- Найдено: ТЗ-038b обрабатывал `payment.succeeded`/`payment.canceled`, но не `refund.succeeded`. Подтверждено на тесте: оплата → Pro → возврат → Pro остался активным бессрочно. Финансовая дыра.
- API ЮKassa: объект `refund` содержит `id` (ID возврата, НЕ платежа), `status`, `payment_id`, `amount`; вебхук — та же обёртка `{"type":"notification","event":"refund.succeeded","object":{...}}`. Частичные возвраты существуют (от 1₽).
- Решение (согласовано с Сашей): отзывать Pro при любом возврате (полном или частичном), без сравнения суммы — проще и безопаснее для бизнеса.
- `backend/app/api/v1/payments.py`:
  - `_revoke_pro(user)` — зеркалит `POST /admin/users/{id}/revoke-pro` (`tier="free"`, `expires_at=None`).
  - `_verify_and_revoke_refund(refund_id, session)` — обратный `GET /v3/refunds/{id}` к API ЮKassa (не доверяем телу вебхука), находит `Payment` по `payment_id`, отзывает Pro. Идемпотентно (уже `status="refunded"` → пропуск).
  - `yukassa_webhook` — принимает `refund.succeeded`; `object.id` для `refund.*` — ID возврата, не платежа (payment_id внутри объекта возврата).
  - `Payment.status` — добавлено значение `refunded` в комментарий (миграция не нужна — поле `str`, схема через `create_all`).

### Verified
- `TestYukassaRefund` (6 тестов): полный цикл оплата→Pro→возврат→отозван; частичный возврат тоже отзывает; идемпотентность повтора; возврат по неизвестному платежу → 400 без краша; тело вебхука врёт о статусе → переверификация не отзывает Pro; отсутствие `object.id` → `no_refund_id` без краша.
- Полный `pytest` — **145 passed, 2 skipped** (было 139, +6, без регрессий). `tsc --noEmit` — 0 ошибок.

### Next step
- Ручное (вне доступа — личный кабинет ЮKassa и прод-БД):
  1. Включить подписку на `refund.succeeded` в личном кабинете ЮKassa (Интеграция → HTTP-уведомления).
  2. Вручную снять Pro со своего тестового аккаунта (`POST /admin/users/{user_id}/revoke-pro` с admin-токеном) — старый вебхук повторно не придёт.
- Alembic миграции; тестирование уведомлений на VPS; перепроверить `shareToTelegram`; webhook URL ЮKassa; per-page lastmod в sitemap — бэклог; уникальный индекс `(page_type, slug, lang)` — бэклог.

---

## 2026-07-08 — Полный аудит проекта + фикс: ежедневный гороскоп в Telegram обрывался на полуслове

### Completed
- Полный функциональный аудит (8 параллельных read-only агентов, каждая находка перепроверена по коду, не по ТЗ/PROGRESS.md). Полный отчёт: `docs/archive/AUDIT_2026-07-07.md` (не читать целиком без необходимости — точечно смотреть нужный раздел). Топ-находки: реферальная программа не работает с момента внедрения (`RegisterRequest` не принимает `ref_code`); возврат Telegram Stars не отзывает Pro (тот же класс бага, что ТЗ-061 для ЮKassa); `/natal/calculate` и `/natal/svg` без авторизации и rate limit; push-уведомления никто не вызывает (только ручной self-test); структурные данные AI-фич (карты/руны/планеты/знаки) только ru/en; Privacy.tsx и Terms.tsx противоречат друг другу по сроку хранения данных (12 мес vs 30 дней — код подтверждает 30).
- Найдено по скриншоту (push "Гороскоп на сегодня"): текст обрывался на середине предложения. Причина — `app/services/horoscope.py:35` (`generate_horoscope`, только для планировщика Telegram-рассылки; интерактивный экран в приложении использует отдельный путь `max_tokens=700`, `horoscope.py:107`, не затронут) — `max_tokens=200` мало для 60-70 слов на кириллице. Тот же класс бага, что 3 раза чинили в ТЗ-060.
- `GENERATION_MAX_TOKENS` вынесен в константу, 200 → **500**. Логирование `finish_reason == "length"`.

### Verified
- `tests/test_horoscope_service.py` (3 теста): регрессия на порог `>= 500`, мок обрезанного/полного ответа. Полный `pytest` — **148 passed, 2 skipped** (было 145, без регрессий).

### Next step
- Alembic миграции (вместо create_all); перепроверить `shareToTelegram` на телефоне; webhook URL ЮKassa; per-page lastmod в sitemap — бэклог; уникальный индекс `(page_type, slug, lang)` — бэклог.
- Приоритизировать находки `AUDIT_2026-07-07.md` в отдельные тикеты: реферальная программа, возврат Stars, `/natal` без auth+rate limit, push, языковой пробел, противоречие Privacy/Terms.
- Подписка на `refund.succeeded` + ручное снятие Pro со своего аккаунта (см. ТЗ-061 выше).

---

## 2026-07-11 — ТЗ-062: фикс UUID в composite_interpret (500 на каждый вызов)

### Completed
- Диагностика (найдено в `docs/archive/AUDIT_2026-07-07.md`, подтверждено эмпирически по запросу Саши) — `compatibility.py:603`: `session.get(UserPartner, req.partner_id)` передавал str вместо UUID. Падало со `StatementError`/`AttributeError: 'str' object has no attribute 'hex'` на каждый вызов, для любого пользователя и раздела — весь AI-интерпретатор Composite Chart (overview/planets/aspects/advice) был нерабочим. Сам расчёт Composite Chart (не интерпретация, `compat_composite`) не затронут — там уже стоял `UUID(req.partner_id)`.
- Добавлен общий `_parse_partner_id(partner_id: str) -> UUID` в `compatibility.py`: оборачивает `UUID()`, превращает `ValueError` в `HTTPException(422, "Invalid partner_id")`. Ни одно из 4 мест разбора `partner_id` в файле раньше не обрабатывало невалидный формат (свалились бы в общий 500) — применено единообразно во всех четырёх местах (строки 208, 228, 526, 603), не только в багованном.

### Verified
- Новый `tests/test_compatibility.py`: невалидный формат partner_id → 422, не 500; partner_id чужого партнёра → 404, не 500. Оба гоняются без Docker.
- По одному тесту на все 4 раздела (overview/planets/aspects/advice) с валидным partner_id — требуют реального kerykeion/pyswisseph (нет Windows-колеса), помечены `skipif` по образцу `test_natal_timezone.py`: локально скипаются, реально прогоняются в Docker/CI.
- Полный `pytest` — **150 passed, 6 skipped** (было 148/2 — +2 passed, +4 skipped, без регрессий).
- `tsc --noEmit` — 0 ошибок. Фронтенд не менялся: `streamRequest`/`parseApiError` уже показывают `body.detail` для любого не-2xx статуса вне 401/402/429/5xx — новый 422 подхватывается существующей веткой без правок. `partner_id` на фронте всегда берётся из списка партнёров (реальный UUID), так что 422 — защитный случай на бэкенде, не реальный пользовательский сценарий.

### Next step
- Прогнать 4 skipped-теста реально в Docker (`docker compose exec backend pytest tests/test_compatibility.py -v`) на сервере — Саша делает деплой/pull сам.
- Alembic миграции; перепроверить `shareToTelegram`; webhook URL ЮKassa; per-page lastmod в sitemap; уникальный индекс `(page_type, slug, lang)` — бэклог.
- Остальные находки `AUDIT_2026-07-07.md`: реферальная программа, возврат Stars, `/natal` без auth+rate limit, push, языковой пробел, Privacy/Terms — в отдельные тикеты.

---

## 2026-07-11 — ТЗ-063: реферальная программа не начисляла бонусы никогда

### Completed
- Диагностика (отдельным заходом, до фикса): бэкенд-логика начисления (`POST /referrals/apply`, `_extend_pro`, `ReferralLog`) уже была полностью реализована и покрыта 5 тестами — разрыв был только в интеграции. `RegisterRequest` не принимал `ref_code`; фронтенд ни разу не вызывал `/referrals/apply`; Telegram-пути (auto-login/widget/merge) вообще не касались реферального кода; `startapp`/`start_param` deep-link не читался совсем (`initDataUnsafe` даже не был в типах).
- Решение: не трогать `RegisterRequest`/`register()` — вместо этого фронтенд сам вызывает уже готовый и протестированный `/referrals/apply` в момент, когда у нового пользователя впервые появляется JWT (email — сразу после `/auth/verify-email`; Telegram — сразу после `/auth/telegram` при `is_new === true`, во всех трёх местах, где дергается этот эндпоинт: auto-login, widget-login, и теперь единообразно).
- `backend/app/api/v1/referrals.py`: rate-limit на `/referrals/apply` (переиспользован `_check_ip_rate`/`_get_ip` из `auth.py`, тот же лимит 5/час, что у `register`); гейт `email_verified` для email-аккаунтов — с явным исключением для Telegram-нативных (`email is None`), иначе Telegram-бонусы никогда бы не начислялись (у них `email_verified` по умолчанию `False` и никогда не станет `True`); коллизия `ref_code` теперь исключена ретраем в `_ensure_ref_code` (колонка не была `unique`).
- Frontend: `utils/telegram.ts` — тип `initDataUnsafe.start_param`; `main.tsx` — мост `start_param` → тот же `localStorage["mystral_ref_code"]`, что и у веб-пути `/ref/CODE`, один механизм применения на оба источника; новый `applyStoredReferralCode()` в `utils/api.ts`; подключён в `VerifyEmail.tsx`, `AuthContext.tsx` (auto-login), `LoginScreen.tsx` (widget-login); убрано мёртвое поле `ref_code` из тела `/auth/register` (бэкенд его больше не читает и не будет). `Profile.tsx` — явная строка `total_bonus_days`.

### Verified
- Новый `TestReferralFullCycle` в `test_profile.py` (4 теста): полный HTTP-цикл email (register→verify→apply, оба получают Pro); неверифицированный email → 400, бонус не начислен; Telegram-нативный пользователь (`email=None`, `email_verified=False`) **не** блокируется гейтом — ключевой тест, доказывающий, что фикс не сломал Telegram; rate-limit на 6-й попытке → 429.
- Полный `pytest` — **154 passed, 6 skipped** (было 150/6, +4 passed, без регрессий).
- `tsc --noEmit` — 0 ошибок.
- Живая ручная проверка в браузере — **выполнена** после того, как Docker поднялся (первая попытка упёрлась в краш Docker Desktop, вторая — в устаревший образ backend без `json-repair`, пересобран `docker compose build backend`). Полный цикл на 2 реальных аккаунтах через настоящий UI: Alice регистрируется → верифицирует email → копирует ссылку `/ref/3ZERN953` → Bob переходит по ней (код лёг в `localStorage`) → регистрируется → верифицирует email → в network-логе браузера виден автоматический `POST /referrals/apply → 200 OK` сразу после `verify-email`. Проверено в БД напрямую: Alice `subscription_tier=pro`, `expires_at=+7д`, `ref_bonus_days_given=7`; Bob `subscription_tier=pro`, `expires_at=+3д`, `referred_by=<Alice.id>`; `referral_log` — 1 строка. В профиле Alice вживую: «Mystral Pro активен · Pro до 18 июля», «Получено дней Pro: +7», счётчик рефералов «1», в списке приглашённых — «Bo\*\*\* +7д».
- Telegram `start_param`-путь — покрыт только `tsc`/код-ревью, не проверен вживую (нужен настоящий Telegram-клиент/бот, недоступно даже с поднятым Docker).

### Next step
- `scheduler.py` `ReferralLog.user_id` краш — уже отдельный тикет-предложение (не в рамках ТЗ-063).
- Тестовые аккаунты `alice@test.com`/`bob@test.com` остались в локальной dev-БД (Docker) — не мешают, можно почистить или оставить для дальнейшего ручного тыканья.
- Остальные находки `AUDIT_2026-07-07.md`: возврат Stars, `/natal` без auth+rate limit, push, языковой пробел, Privacy/Terms — в отдельные тикеты.

---

## 2026-07-11 — Деплой ТЗ-063 упирался в диск на VPS + фикс Dockerfile backend

### Completed
- При деплое ТЗ-063 на прод: `docker compose build` падал на `no space left on device` (сначала на экспорте образа, `journald` тоже не мог писать) — корневой раздел VPS `/dev/vda2` всего 9.8G, был забит на 94%.
- `docker system prune -a -f` + `docker builder prune -a -f` освободили 1.269GB (94%→86% занято) — хватило на фронтенд, но не на backend: тот же файл (`timezonefinder/data/boundaries/coordinates.fbs`) не помещался при экспорте образа снова.
- Причина — `backend/Dockerfile` был однослойный: `build-essential`/`gcc`/`libssl-dev` (нужны только чтобы собрать `pyswisseph`/`cryptography` из исходников) оставались в финальном образе навсегда. Переведён на multi-stage: сборка в отдельном `builder`-стейдже (`pip install --prefix=/install`), в финальный слой копируется только `/install` — без компилятора.

### Verified
- Локально: сборка чистая, content size образа **292MB → 168MB**. Контейнер поднят, планировщик стартовал без ошибок (значит скомпилированные расширения по-прежнему подгружаются корректно — рантайм-либы уже есть в `python:3.11-slim`), `/docs` отвечает 200.
- На проде: `./deploy.sh` прошёл до конца, задеплоилось, коммиты (ТЗ-062, ТЗ-063, этот фикс) на месте — подтверждено пользователем.

### Next step
- Диск на VPS всё ещё около 86% занят даже после чистки — на будущее, если стек продолжит расти, может понадобиться либо расширение диска у провайдера, либо сборка образов вне VPS (registry + pull вместо build на месте). Пока работает, отдельным тикетом не заводил.

## 2026-07-12 — ТЗ-064: возврат Telegram Stars не отзывал Pro-статус

### Completed
- **Диагностика показала две проблемы, не одну.** В `bot/main.py` не было вообще
  никакого обработчика события возврата (в отличие от ТЗ-061, где вебхук
  ЮKassa приходил, но `refund.succeeded` не было в белом списке событий —
  здесь просто нет хендлера). Но глубже: `stars_activate` в
  `backend/app/api/v1/payments.py` никогда не создавал строку в `payments`
  для Stars-покупок — только флоу ЮKassa пишет туда. Значит даже если бы
  обработчик возврата был, искать в БД было бы нечего.
  - Проверил живьём пакет `aiogram==3.10.0` (версия, закреплённая в
    `bot/requirements.txt`) — распаковал wheel, `RefundedPayment` и поле
    `Message.refunded_payment` там уже есть. Апгрейд библиотеки не нужен.
  - По актуальному Bot API возврат Stars-платежа — не отдельный тип апдейта,
    а поле `refunded_payment` на обычном `message`-апдейте (та же механика,
    что у `successful_payment`). Приходит и при обращении пользователя в
    Telegram напрямую, и при вызове ботом `refundStarPayment`. Метод
    `refundStarPayment` принимает только `user_id` + `telegram_payment_charge_id`,
    без суммы — частичных возвратов через него не бывает, но политику
    ТЗ-061 (отзыв Pro при любом возврате, без сравнения сумм) применил и
    здесь для единообразия и на случай, если механизм когда-то изменится.
- **Сделано:**
  - `_record_stars_payment()` (`payments.py`) — пишет `Payment(provider="stars")`
    при активации, идемпотентно по `provider_payment_id=telegram_payment_charge_id`
    (retry активации не даёт дубликат и не падает на уникальном индексе).
    `StarsActivateRequest` получил новое поле `telegram_payment_charge_id`
    (опционально, пусто = старое поведение без записи, с warning в лог).
  - `POST /stars/refund` (внутренний, тот же `_require_internal_token`, что у
    `/stars/activate`) — ищет `Payment` по `telegram_payment_charge_id`,
    отзывает Pro через уже существующий `_revoke_pro` (тот же хелпер, что
    ТЗ-061 завёл для ЮKassa). Идемпотентно: платёж уже `status="refunded"`
    пропускается молча.
  - `bot/main.py`: `on_successful_payment` теперь передаёт
    `telegram_payment_charge_id` в `/stars/activate`; новый хендлер
    `on_refunded_payment` (`F.refunded_payment`) шлёт `charge_id` в
    `/stars/refund` с тем же паттерном retry (3 попытки), что у активации,
    и алертит в поддержку при неудаче — отдельно различая "платёж не найден
    в БД" (старые Stars-покупки до этого фикса, см. ручной шаг ниже) от
    "ошибка сервера".
  - В отличие от вебхука ЮKassa, тут не нужна повторная верификация через
    API провайдера — `refunded_payment` приходит через собственный
    long-polling канал бота, уже аутентифицированный токеном бота, а не
    через публичный незащищённый URL.

### Verified
- Новый класс `TestStarsRefund` (4 теста) в `test_payments.py`: полный цикл
  оплата→Pro→возврат→Pro отозван; идемпотентность повторной доставки;
  неизвестный/несуществующий платёж → 404 без краша; эндпоинт требует
  внутренний токен. Плюс 2 теста на саму запись `Payment` при активации
  (persists row + graceful degradation без charge_id).
  Полный `pytest` — **160 passed, 6 skipped** (было 145, +15 новых, регрессий
  нет). Фронтенд не затронут.

### Manual steps required
- **Настройка Telegram НЕ требуется** (в отличие от ЮKassa, где нужно было
  включить подписку на `refund.succeeded` в личном кабинете) —
  `refunded_payment` приходит как обычный `message`-апдейт, а бот уже
  поллит `message`-апдейты, дополнительный `allowed_updates` не нужен.
- **Все Stars-покупки, сделанные ДО этого фикса, не имеют строки `Payment`**
  (activate их не записывал). Если такая старая покупка будет возвращена,
  бот получит 404 от `/stars/refund`, пришлёт алерт в поддержку с `tg_id`
  из события — Pro для этого пользователя нужно будет отозвать вручную через
  `POST /admin/users/{user_id}/revoke-pro` (тот же путь, что был описан в
  ручных шагах ТЗ-061).

### Next step
- Backfill `Payment` строк для старых Stars-покупок, если найдутся способ
  восстановить `telegram_payment_charge_id` из истории (Telegram Bot API не
  отдаёт список прошлых платежей ботом — скорее всего backfill невозможен,
  остаётся только ручная обработка по факту обращения).
- Alembic-миграции вместо `create_all` — тянется с ТЗ-061, всё ещё в бэклоге.

## 2026-07-12 — ТЗ-065: единая система модерации возвратов (ЮKassa + Stars)

### Completed
- **Контекст:** до этого тикета `/refund-request` (ТЗ-051) только отправлял
  email — не создавал структурированную заявку и не инициировал реальный
  возврат. Реальные возвраты ЮKassa делались вручную через личный кабинет;
  возвратов Stars через код не было вообще. Автоматический отклик на уже
  случившийся возврат (отзыв Pro) — ТЗ-061/ТЗ-064, не трогали, переиспользовали.
- **Новая таблица `RefundRequest`** (`backend/app/models/user.py`):
  `user_id`, `payment_id` (ссылка на `Payment`, работает для обоих
  провайдеров через `provider`), `status` (pending/rejected/completed/failed),
  `reason`, `admin_comment`, `error_detail`, `resolved_at`, `resolved_by`.
  **Решение:** статус `approved` из ТЗ убрал из рантайм-схемы — одобрение и
  вызов провайдера происходят атомарно в одном клике администратора, поэтому
  отдельного "одобрено, но ещё не возвращено" состояния не возникает;
  результат сразу `completed` или `failed`.
  - Модуль 2 (`backend/app/api/v1/payments.py`): `/refund-request` теперь
    сам находит подходящий платёж (`_find_refundable_payment` — последний
    `succeeded` платёж пользователя без открытой/завершённой заявки на него)
    и создаёт `RefundRequest(pending)`. **Решение:** не стал делать
    пользователю выбор платежа из списка (как буквально просили) — на
    практике у пользователя почти всегда один актуальный платёж; авто-выбор
    последнего избавляет от лишнего UI ради редкого кейса. `reason` оставил
    опциональным (как было). Новый `GET /refund-request/status` отдаёт
    статус последней заявки пользователя.
  - Модуль 3 (`backend/app/api/v1/admin.py`): `GET /admin/refunds` (pending +
    история), `POST /admin/refunds/{id}/approve`, `POST /admin/refunds/{id}/reject`
    (обязательный `admin_comment`). Approve вызывает провайдера
    (`yukassa_issue_refund` / `stars_issue_refund` в `payments.py`, реюзаны
    админкой) и переводит заявку в `completed`/`failed` по факту ответа —
    **отзыв Pro НЕ делается здесь напрямую**, а происходит автоматически через
    уже существующие `refund.succeeded`-вебхук (ТЗ-061) / `refunded_payment`
    бот-хендлер (ТЗ-064), чтобы не задваивать идемпотентную логику.
  - Провайдерские вызовы никогда не бросают исключение — возвращают
    `(success, error_detail)`, поэтому ошибка провайдера (например,
    «уже возвращено») превращается в чистый `status=failed` с `error_detail`,
    а не в 500. Дополнительно: если `Payment.status` уже `"refunded"` до
    вызова (возврат сделали в обход системы, вручную в кабинете ЮKassa) —
    короткое замыкание без обращения к провайдеру вообще.
  - Email (`backend/app/core/email.py`): `send_refund_email` расширен
    провайдером/суммой платежа; новый `send_refund_decision_email` —
    «возврат оформлен» либо «отклонён» + `admin_comment`.
- **Фронтенд:**
  - `Profile.tsx` — текстовое поле причины (необязательное) + статус своей
    заявки (pending/rejected+комментарий/completed/failed) над кнопкой,
    подгружается через новый `GET /refund-request/status`.
  - `Admin.tsx` — новая вкладка «Возвраты»: список на рассмотрении (с
    провайдером/суммой/причиной) с кнопками «Одобрить и вернуть»/«Отклонить»
    (комментарий через `window.prompt`, как остальные деструктивные действия
    в этой панели), плюс история решённых заявок.
  - 6 локалей (ru/en/tr/es/pt/uk) — новые ключи `refund_reason_placeholder`,
    `refund_status_*`, `refund_no_payment`.

### Verified
- Новый файл `backend/tests/test_admin_refunds.py` (14 тестов): список
  требует токен; approve ЮKassa (мок) → completed; approve Stars (мок) →
  completed; Stars без привязанного Telegram-аккаунта → failed с понятной
  причиной; ошибка провайдера (400 "already refunded") → failed, не 500;
  уже `refunded` платёж → короткое замыкание, провайдер не вызывается вообще
  (проверено счётчиком вызовов); двойное approve/reject → 400 на второй раз;
  reject без комментария → 422; reject успешно → `rejected` + Pro не тронут.
  `TestRefund` в `test_payments.py` переписан под платёж-ориентированную
  модель (было по `User.subscription_created_at`, стало по `Payment.created_at`)
  + 3 новых теста (нет платежа под Pro → 400, статус пуст изначально,
  статус отражает pending). Полный `pytest` — **177 passed, 6 skipped**
  (было 160, +17 новых, регрессий нет). `tsc --noEmit` — 0 ошибок.
- Фронтенд: поднял dev-сервер (`npm run dev`) и проверил в браузере —
  вкладка «Возвраты» рендерится, пустые состояния корректны
  («Нет заявок на рассмотрении» / «Пусто»), ошибок в консоли нет, Vite-прокси
  верно достаёт до нового `/api/v1/admin/refunds` (просто ECONNREFUSED —
  бэкенда в этой сессии не было, Docker Desktop не запущен). Живой e2e-прогон
  через реальный бэкенд (оплата → заявка → approve → реальный вызов
  ЮKassa/Telegram → вебхук отзывает Pro) **не выполнялся** — недоступен
  Postgres/Redis в этой сессии. Логика полностью покрыта автотестами, но
  живая проверка на VPS/локальном docker-compose — must-do перед тем, как
  считать фичу боевой.

### Manual steps required
- Живой прогон полного цикла на реальном стенде (см. выше) — Docker не
  поднимался в этой сессии.
- Ничего нового на стороне ЮKassa/Telegram настраивать не нужно — approve
  использует те же ключи/токен, что уже были в ТЗ-038b/055/064.

### Next step
- Живой e2e-прогон на VPS/docker-compose перед тем, как полагаться на фичу
  в проде.
- Alembic-миграции вместо `create_all` — всё ещё в бэклоге (ТЗ-061/064/065
  все трогают схему через `create_all`).

## 2026-07-12 — ТЗ-065 доп.: живой прогон на docker-compose нашёл 2 реальных бага

- Саша поднял Docker Desktop — прогнал полный цикл через реальные
  Postgres/Redis/backend (не SQLite из тестов). Живые находки:
  1. **`DELETE /admin/users/{id}` падал 500** на любом пользователе с
     платежом — ручной cascade-список в `delete_user` (`backend/app/api/v1/admin.py`)
     никогда не включал `payments` (пробел с ТЗ-038b/055, просто не был
     покрыт тестами) и тем более не знал про новый `refund_requests`.
     Postgres реально проверяет FK (`payments_user_id_fkey`), SQLite в
     тестах — нет, поэтому пробел был невидим до живого прогона.
  2. **Тот же cascade-цикл никогда не удалял ни одной строки на SQLite** —
     сырой `text("DELETE ... WHERE user_id = :uid")` с `{"uid": user_id}`
     (питоновский `UUID`) падает на aiosqlite ("type 'UUID' is not
     supported"); `str(user_id)` (с дефисами) не падает, но и не матчится —
     SQLite хранит UUID-колонки как 32-символьный hex БЕЗ дефисов.
     Эмпирически проверил формат хранения и поправил на `user_id.hex` —
     подходит и SQLite (совпадает формат), и Postgres (uuid-тип принимает
     hex без дефисов на входе).
  - Добавлен `TestDeleteUserCascade` (`test_admin_refunds.py`) — раньше
    `delete_user` вообще не имел тестового покрытия. Полный `pytest` —
    **178 passed, 6 skipped**.
  - Отдельно, во время живой проверки заметил: у этого бэкенда кастомный
    global exception handler (`main.py`) отдаёт ошибки как
    `{"error": status, "message": "..."}`, а не FastAPI-дефолтный
    `{"detail": "..."}`. Моя новая строка в `Profile.tsx` читала `d.detail`
    (никогда не сработало бы) — поправил на `d.message || d.detail`. Это
    системный паттерн по всему фронтенду (LoginScreen, NatalChart,
    ForgotPassword, ResetPassword и другие места в самом Profile.tsx читают
    `.detail` тем же образом) — чинил только свою новую строку, остальное
    вне периметра этого тикета.
  - Живьём подтверждён полный цикл модерации: заявка → статус в профиле →
    список в админке → reject с комментарием (+ защита от повторного) →
    короткое замыкание на уже возвращённом платеже (+ защита от повторного
    approve) → каскадное удаление пользователя с платежами и заявками.
  - **Не проверялся** живой успешный `approve` с реальным списанием у
    провайдера — auto-mode классификатор корректно заблокировал попытку
    дёрнуть боевые API ЮKassa/Telegram фиктивными ID платежей ("Real World
    Transactions"), поскольку это реальный вызов финансового провайдера
    боевыми ключами, на который Саша явно не давал добро (просил "прогнать
    локально", не "вызвать провайдера"). Эта ветка остаётся проверенной
    только моками в автотестах.
  - Тестовые пользователи/платежи, созданные для проверки, удалены из БД.
    `.env` не имел `ADMIN_TOKEN` вообще (админка и внутренние эндпоинты бота
    были недоступны локально даже без учёта ТЗ-065) — по просьбе Саши
    оставил токен настроенным (сгенерирован `secrets.token_urlsafe(32)`).
    Docker-контейнеры остановлены после проверки по его просьбе.

## 2026-07-13 — Ежедневный гороскоп: обрыв текста рецидивировал + Markdown-«**» пролезал в HTML

- **По двум скринам от Саши** (push-уведомления "Гороскоп на сегодня"):
  1. Текст обрывался на полуслове у Скорпиона ("...доведи" и всё) —
     подозрительно похоже на баг, уже чинившийся 8 июля (`GENERATION_MAX_TOKENS`
     200→500). Попросил Сашу проверить прод-логи — подтвердилось:
     `Daily horoscope for scorpio/ru truncated by max_tokens
     (finish_reason=length)`, 2026-07-13 06:00 UTC. 500 всё равно не хватило.
  2. У Овна литерально показывалось `**работа**` вместо жирного текста.
- **Root cause обрыва (не гадал, поднял историю ТЗ-060):** тот же класс бага,
  что там чинили трижды подряд — модель тратит бюджет токенов на
  саморассуждение/самокоррекцию прямо в тексте ответа до того, как выдаст
  финальный вариант, а не только кириллица требует больше токенов на слово.
  Урок ТЗ-060 (`ece3796`, `ff82a3a`, `09e8524`) — перестать подбирать число по
  чуть-чуть и (а) явно запретить рассуждения вслух в промпте, (б) дать щедрый
  потолок токенов, раз это не стоит дороже при более коротком реальном ответе.
- **Root cause Markdown:** рассылка шлётся с `parse_mode: "HTML"`
  (`app/scheduler.py`), а системный промпт нигде не запрещал модели
  Markdown-разметку — HTML-режим Telegram `**bold**` не понимает вообще,
  показывает как есть.
- **Сделано:**
  - `backend/app/services/horoscope.py`: `GENERATION_MAX_TOKENS` 500 → 2000
    (щедрый потолок, не дороже при коротком ответе). В промпт (ru и en/прочие
    ветки) добавлен явный запрет на рассуждения вслух/самокоррекцию
    ("хотя нет", "точнее", "wait, actually") и на Markdown-разметку —
    формулировка взята из уже проверенного анти-rambling промпта SEO-генерации.
  - `backend/app/scheduler.py`: новая `_sanitize_llm_text()` — подстраховка
    поверх промпт-фикса на случай, если модель всё равно проигнорирует
    инструкцию: экранирует HTML-метасимволы (иначе случайный `<`/`&` от модели
    мог бы вообще сломать парсинг Telegram, не просто криво отрендериться) и
    конвертирует `**bold**` → `<b>bold</b>`. Применяется в `_format_message`
    до сборки финального сообщения.
- **Проверено:** `test_horoscope_service.py` — порог поднят до `>= 2000`,
  новый тест на наличие анти-Markdown инструкции в промпте (оба языковых
  пути). Новый `test_scheduler.py` (4 теста) — конвертация `**bold**`,
  обычный текст не трогается, HTML-метасимволы экранируются, несколько
  bold-фрагментов в одном тексте. Полный `pytest` — **183 passed, 6 skipped**
  (было 178, регрессий нет).
- **Требуется деплой** — фикс только в git, на VPS ещё не выкатан
  (`./deploy.sh`), реальный эффект в проде появится после следующего деплоя.
